<h1>All pets</h1>
<ul>
  <% @pets.each do |pet| %>
    <li> <%= link_to pet.name, pet_path(pet) %> (<%= pet.species %>) - Found on <%= pet.found_on %> at <%= pet.address %> </li> 
  <% end %>
</ul>

<%= link_to "Add a pet", pets_new_path %>
